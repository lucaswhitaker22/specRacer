{
  "name": "text-racing-mmo",
  "version": "1.0.0",
  "description": "A browser-based text-driven MMO racing game",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test:all": "npm run test --prefix backend && npm run test --prefix frontend"
  },
  "keywords": ["racing", "mmo", "game", "websocket", "vue", "nodejs"],
  "author": "",
  "license": "MIT"
}